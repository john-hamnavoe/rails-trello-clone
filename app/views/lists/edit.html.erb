

<%= turbo_frame_tag dom_id(@list) do %>
  <div class="bg-white rounded-lg shadow-md border-blue-200 border-2 p-2 max-w-xl">
    <h1 class="text-base font-semibold text-gray-900">Update list</h1>
    <%= render 'form', list: @list, board: @board, cancel_path: board_path(@list.board), data: {turbo_frame: :_top}   %>
  </div>
<% end %>