 <% content_for :title do %>
  Boards
 <% end %>

<div class="mt-2 flex mb-4">
  <%= render Button::PrimaryComponent.new(text: "New Board",path: new_board_path) %>
</div>

<div class="container mx-auto rounded-lg bg-gray-100 p-4">
  <div class="grid grid-cols-1 sm:grid-cols-3 gap-4">
    <% @boards.each do |board| %>
      <%= render LinkCardComponent.new(title: board.name, path: board_path(board)) %>
    <% end %>
  </div>
</div>

